<div class="input-field input-field-select {{disabled}}">
    <p class="label">{{label}}</p>
    <div class="input-row">
        <input type="{{type}}" name="{{name}}" class="input" id="{{name}}" placeholder="{{placeholder}}" {{disabled}}>
    </div>
    <p class="error-msg"></p>
</div>