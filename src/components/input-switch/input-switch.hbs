<div class="input-switch {{disabled}}">
    <span class="label">{{label}}</span>
    <label class="switch">
        <input type="checkbox" name="{{name}}" id="{{name}}" {{checked}} {{disabled}}>
        <span class="slider"></span>
    </label>
</div>